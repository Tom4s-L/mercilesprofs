{% extends 'base.html.twig' %}

{% block title %}MerciLesProfs !{% endblock %}

{% block main %}
<div id="welcome">

<div id="text-welcome">
<h1 class="font-jedi green text-5xl">Merci les profs</h1>

</div>

</div>

{% if posts is not empty %}

    {% for post in posts %}

        <div>
            <p>
                Ecrit par: {{post.author}}
            </p>
            <p>
                {{post.content}}
            </p>
        </div>
        
    {% endfor %}

{% else %}

    <p>Pas de posts pour l'instant, <a href="{{ path('post_add') }}">créez en un</a></p>

{% endif %}

    <div class="paging d-flex justify-content-center">
        {{ knp_pagination_render(posts, 'paging.html.twig') }}
    </div>

{% endblock %}
